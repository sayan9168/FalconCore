[package]
name = "falconcore"
version = "1.1.0"
edition = "2021"
description = "FalconCore — a security-minded, embeddable programming language runtime and developer toolchain."
license = "Apache-2.0"
readme = "README.md"

[dependencies]

[profile.release]
lto = true
codegen-units = 1
panic = "abort"
